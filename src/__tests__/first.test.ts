describe('First', () => {
  it('must be possible sum 2 numbers', () => {
    expect(1 + 1).toEqual(2);
  });

  it('must be possible sum 2 numbers', () => {
    expect(2 + 2).not.toEqual(2);
  });
});
